services:
  - type: web
    name: iris-flask-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/main.py"
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8000
